<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
      body {
        margin: 40px auto;
        max-width: 650px;
        line-height: 1.6;
        font-size: 18px;
        color: #e0e0e0; /* Soft white */
        background-color: #2b2b2b; /* Dark grey */
        padding: 20px;
        font-family: Arial, sans-serif;
      }
      h1, h2, h3 {
        line-height: 1.2;
        color: #f5f5f5; /* Slightly brighter white for headings */
      }
      a {
        color: #a8dadc; /* Soft teal for links */
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      section {
        margin-top: 30px;
        padding: 15px;
        background-color: #3a3a3a; /* Slightly lighter grey for section backgrounds */
        border-radius: 8px;
      }
      section h2 {
        margin-top: 0;
      }
      ul {
        list-style: none;
        padding: 0;
      }
      ul li {
        margin-bottom: 10px;
      }
    </style>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>

